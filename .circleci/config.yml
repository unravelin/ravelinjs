version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: cimg/node:10.24

    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: "Env?"
          command: env




